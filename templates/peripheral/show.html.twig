{% extends 'base.html.twig' %}

{% block title %}Peripheral{% endblock %}

{% block body %}
    <h1>Peripheral</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ peripheral.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ peripheral.type }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ peripheral.price }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ peripheral.name }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ peripheral.brand }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ peripheral.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('peripheral_index') }}">back to list</a>

    <a href="{{ path('peripheral_edit', {'id': peripheral.id}) }}">edit</a>

    {{ include('peripheral/_delete_form.html.twig') }}
{% endblock %}
